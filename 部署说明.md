# 人事管理系统 部署说明

本项目是 **Spring MVC + MyBatis + JSP** 的 Maven **war** 工程，下面给出 3 种部署方式：

- 方式一：内置 Tomcat 一键运行（最简单，适合本地开发/演示）
- 方式二：IDEA 配置本地 Tomcat 运行（适合开发调试）
- 方式三：打 war 包部署到独立 Tomcat 服务器（适合正式/服务器部署）

---

## 〇、环境要求

| 软件 | 版本 | 说明 |
| ---- | ---- | ---- |
| JDK | **8**（推荐） | Spring 4.2 与 JDK 8 最匹配；Lombok 已升到 1.18.34，JDK 8~21 均可编译 |
| Maven | 3.6+ | 构建工具 |
| MySQL | 8.x | 也可用 MariaDB 10；驱动为 `mysql-connector-java 8.0.29` |
| Tomcat | **9**（方式三需要） | 与内置插件一致；JDK 8 环境 |

> ⚠️ **重要：本项目必须部署在「根路径 /」下。**
> 页面里的链接都是绝对路径（如 `/dept/list.do`、`/login/login.do`、`/css/app.css`）。
> 如果部署成 `http://ip:8080/angongye/` 这种带项目名的路径，所有链接都会失效。
> 方式一/二默认就是根路径；方式三要按下文把 war 部署为 `ROOT.war`。

---

## 一、准备数据库（三种方式都要做）

1. 启动 MySQL，执行项目自带脚本 `src/main/resources/angongye.sql`：

   ```bash
   mysql -u root -p < src/main/resources/angongye.sql
   ```

   （也可以在 Navicat / IDEA Database 工具里打开该文件直接运行。）
   脚本会自动：建库 `angongye` → 建三张表（`t_dept`/`t_emp`/`t_login`）→ 写入演示数据。
   脚本开头的 `SET NAMES utf8mb4` 可避免中文乱码。

2. 修改数据库连接 `src/main/resources/db.properties`，把用户名/密码改成你本机的：

   ```properties
   db.mysql.url=jdbc:mysql://localhost:3306/angongye?allowPublicKeyRetrieval=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai&rewriteBatchedStatements=true
   db.mysql.username=root
   db.mysql.password=你的MySQL密码
   ```

---

## 二、方式一：内置 Tomcat 一键运行（推荐，最省事）

`pom.xml` 已内置 `tomcat9-maven-plugin`（端口 **8088**，路径 `/`）。

```bash
# 在项目根目录执行
mvn clean tomcat9:run
```

启动后浏览器访问：<http://localhost:8088/>
默认账号 **zhangsan** / 密码 **123456**（登录需输入图片验证码，纯数字）。

停止：在终端按 `Ctrl + C`。

---

## 三、方式二：IDEA 配置本地 Tomcat

1. IDEA 打开项目，等待 Maven 依赖下载完成。
2. `File → Project Structure → Project SDK` 选择 **JDK 8**。
3. `Run → Edit Configurations → + → Tomcat Server → Local`，
   在 `Application server` 里指向本机 Tomcat 9 安装目录。
4. 切到 `Deployment` 标签 → `+ → Artifact` → 选择 `angongye:war exploded`，
   把 **Application context 设为 `/`**（必须是根路径）。
5. 点绿色 ▶ 启动，访问 <http://localhost:8080/>（IDEA 默认 8080）。

---

## 四、方式三：打 war 包部署到独立 Tomcat 服务器

适合部署到 Linux 服务器 / 正式环境。

### 1. 打包

```bash
mvn clean package -DskipTests
```

生成的 war 包在 `target/mvc_1201.war`。

### 2. 部署为根路径（关键）

因为项目必须跑在根路径，把 war 改名为 `ROOT.war` 放进 Tomcat：

```bash
# 先删除 Tomcat 自带的默认 ROOT 应用
rm -rf $CATALINA_HOME/webapps/ROOT
# 拷贝并改名为 ROOT.war
cp target/mvc_1201.war $CATALINA_HOME/webapps/ROOT.war
```

> 也可以不改名，改用 `conf/server.xml` 里配置 `<Context path="" docBase="mvc_1201"/>`，
> 但改名 `ROOT.war` 是最简单可靠的做法。

### 3. 确认服务器环境

- Tomcat 用 **JDK 8** 启动（`JAVA_HOME` 指向 JDK 8）。
- 服务器上要有可访问的 MySQL，并已执行第一步的 `angongye.sql`。
- war 里打进去的 `db.properties` 用的是 `localhost` 数据库。
  若数据库在别的机器，需在**打包前**改好 `db.properties` 的 IP/账号密码再打包，
  或解压 war 修改 `WEB-INF/classes/db.properties` 后重新打包。

### 4. 启动并访问

```bash
$CATALINA_HOME/bin/startup.sh      # Windows 用 startup.bat
```

访问 `http://服务器IP:8080/`，登录 `zhangsan` / `123456`。

日志查看：`$CATALINA_HOME/logs/catalina.out`。

---

## 五、常见问题排查

| 现象 | 原因与解决 |
| ---- | ---- |
| 页面能打开但点链接 404 / 样式错乱 | 没部署在**根路径**。按方式三改名 `ROOT.war`，或把 context 设为 `/` |
| 中文显示成乱码（如 `å¼ ä¸‰`） | 导入 SQL 时客户端字符集不是 utf8mb4。用 `mysql --default-character-set=utf8mb4 -uroot -p < angongye.sql` 重新导入 |
| 启动报 `Access denied` / 连接不上库 | `db.properties` 的用户名密码不对，或库未创建。检查并重跑 `angongye.sql` |
| 启动报 Lombok/编译错误 | JDK 版本问题。建议用 JDK 8；本项目 Lombok 1.18.34 可兼容到 JDK 21 |
| 登录提示"验证码不正确" | 图片验证码是纯数字，按图输入即可；看不清点验证码图片可刷新 |
| 请求 jsp 变成文件下载 | 缺少 jsp/jstl 依赖，本项目 pom 已包含，正常打包即可 |

---

## 六、默认账号

- 登录名：**zhangsan**
- 密码：**123456**
- 登录后可进入「人事管理 → 部门管理 / 员工管理」进行增删改查。
