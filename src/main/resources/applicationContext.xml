<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:aop="http://www.springframework.org/schema/aop"
       xmlns:tx="http://www.springframework.org/schema/tx"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
           http://www.springframework.org/schema/beans/spring-beans-3.2.xsd
           http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-3.2.xsd
           http://www.springframework.org/schema/aop http://www.springframework.org/schema/aop/spring-aop-3.2.xsd
           http://www.springframework.org/schema/tx http://www.springframework.org/schema/tx/spring-tx-3.2.xsd
            http://www.springframework.org/schema/mvc http://www.springframework.org/schema/mvc/spring-mvc-3.2.xsd">
   <!-- 处理spring的配置 -->
  
    <!-- 合并文件  -->
    <import resource="classpath:springmvc-config.xml"/>
    <!-- 自动扫描基础包 ,这里写的是父包，它将会扫描该包下的所有子包以及类 改 -->
   <context:component-scan base-package="com.angongye"></context:component-scan>
    <aop:aspectj-autoproxy/>
   <!-- 加载数据库的配置文件 -->
   <context:property-placeholder location="classpath:db.properties"/>
	<!-- 其他配置参见 https://developer.aliyun.com/article/327334 -->
	<!-- 创建数据源（新） alibaba -->
   <bean id="dataSource" class="com.alibaba.druid.pool.DruidDataSource" destroy-method="close">
       <property name="url" value="${db.mysql.url}" />
       <property name="username" value="${db.mysql.username}" />
       <property name="password" value="${db.mysql.password}" />
       <property name="driverClassName" value="${db.mysql.driver}" />
       <property name="minEvictableIdleTimeMillis" value="300000"/>
       <property name="maxEvictableIdleTimeMillis" value="600000"/>
   </bean>
   <!-- 创建sessionFactory -->
    <bean id = "sqlSessionFactory" class="org.mybatis.spring.SqlSessionFactoryBean">
	      <!-- 指定mybatis主配置文件 -->
          <property name="configLocation" value="classpath:mybatis-config.xml"/>
          <!-- 利用连接池 本次使用阿里的连接池 -->
          <property name="dataSource" ref="dataSource"/>
          <!-- 别名设置 改 -->
	      <property name="typeAliasesPackage" value="com.angongye.entity"/>
	 </bean>
   <!-- 使用代理对象，获得所有的Mapper -->
   <!-- 匿名bean对象 -->
	 <bean  class="org.mybatis.spring.mapper.MapperScannerConfigurer">
	      <!-- DAO 接口所在包，Spring 会把该包下所有接口扫描为 DAO 代理对象（第14章 DAO 模式） -->
	      <property name="basePackage" value="com.angongye.dao"/>
	      <property name="sqlSessionFactoryBeanName" value="sqlSessionFactory"/>
	 </bean>
   <!-- 设置默认是数据库事务管理 -->
    <!-- 内部含有doBegin  doCommit  doRollback等一些事务控制方法 -->
	 <bean id = "txManager" class="org.springframework.jdbc.datasource.DataSourceTransactionManager">
	 	  <property name="dataSource" ref="dataSource"/>
	 </bean>
	 <!-- 配置到Spring的事务管理器中 -->
	 <tx:annotation-driven transaction-manager="txManager"/>
</beans>           
